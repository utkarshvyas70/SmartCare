/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.pexels.com', 'images.unsplash.com', 'res.cloudinary.com', "images.openfoodfacts.org","upload.wikimedia.org","images.rawpixel.com","www.pexels.com"],
    },
};

export default nextConfig;
